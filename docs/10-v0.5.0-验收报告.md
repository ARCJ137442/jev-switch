# Jev-Switch · v0.5.0 验收报告（全量 DoD 复跑 · P2-6）

> **日期**：2026-09-23 · **执行**：Phase2-Agent · **基线**：`b4c5553` → 本报告前链 `69e0489`（smoke）· `b8fb143`（example）· `1fde82e`（README）
> **性质**：**复跑实证报告**。所有计数与 PASS 均为当日本机实跑输出；`docs/contracts/**` **一字未改**（勾选权留契约所有者/用户）。
> **口径**：✅ = 有当日实跑证据 / 测试名 / commit SHA；⏳ = 实现已落但契约复选框未勾（或需交互实测）；**P2-7** = 留给终局 CDP + 人工实测；SKIP = 依赖本机环境（Vercel key / Laya 服务）未满足。
>
> **2026-09-27 历史状态校正：**本报告记录的是 2026-09-23 的验收候选结论。仓库后来出现 `v0.5.0-stable` 源码 tag，但没有同名 GitHub Release；当前正式 Release 是 `v0.1.0`，不得把此历史报告当作当前版本或发布状态。校正：GPT-6 Luna xhigh（OpenAI Codex），AI 辅助整理。

---

## 〇、当日门禁总览（先跑后写）

| # | 门禁 | 命令 | 结果 |
|---|---|---|---|
| 1 | workspace 测试 | `cargo test --manifest-path rs/Cargo.toml --workspace` | ✅ **142/142**，exit 0 |
| 2 | ts-rs 生成物 | `cargo test … --workspace --features ts-rs` | ✅ **165/165**，exit 0；`git diff ui/src/generated rs/crates/jev-protocol/bindings` = **空** |
| 3 | 黄金测试（仓外） | `powershell -File rs/scripts/golden-test.ps1` | ✅ **3/3**，`== GOLDEN TEST PASSED ==`（`E:\tmp\jev-golden\my_lab_adapter`） |
| 4 | UI lint / build | `npm run lint --prefix ui` · `npm run build --prefix ui` | ✅ tsc 0 错；vite 55 modules → dist（929ms） |
| 5 | 端到端 smoke | `powershell -File scripts/smoke.ps1` | ✅ **PASS 9/9, SKIP 1**，exit 0（SKIP = Laya 18765 未启动的真实推理步） |

smoke 双实现（P2-1，同一断言集）当日在本机**均实跑通过**：

- `scripts/smoke.ps1` → `PASS 9/9, SKIP 1`（步骤 9：probe HTTP 200×2，Vercel 可达 1/2、Laya `ok:false` warning）
- `scripts/smoke.sh` → `PASS 9/9, SKIP 1`（Git Bash 实跑；修掉 Windows jq.exe **CRLF 输出**污染 id/比较的坑后全绿）

---

## 一、08 §4 后端 A 验收逐条

| 条目 | 判定 | 证据 |
|---|---|---|
| **P0-1** 外部 `cargo add --path …/jev-protocol` 后可 `use` 编译 | ✅ | workspace 拆分 `10c2fa9`；黄金测试以 **path-dep** 引 `jev-protocol`+`jev-core` 仓外编译并 3/3（`golden-test.ps1` 当日 PASS：`invoke_zero_kernel_modification_yields_standard_jev_response` 等 3 测）；另 `examples/basic_use.rs`（`b8fb143`）当日 `cargo run --example basic_use` 输出 `noul_probability = 0.69` |
| **P0-2** `{"type":"boolean","probability":0.69}` → `noul≈0.69`（非 0.95） | ✅ | 修复 `6af3a47`；单测 `noul_fixture_boolean_probability_069`（jev-protocol）+ 全路径 `vercel_probability_069_full_path`（jev-adapters）+ example 断言；workspace 142 绿含上述 |
| **P0-3** 协议与 jev-life `types.ts` 对齐（差异仅 Rust 命名） | ⏳（实现✅，diff 勾选留所有者） | 破坏性对齐 `fc91ffa`（Criteria 三形态 / Answer 联合 / 布尔无 confidence / Usage 双拼写 / `noul_probability()`）；ts-rs 生成物 `7735cda`+`5f8a74c`，CI diff 门禁在（`656b4d4`）。契约 `01 §8` 第 5 项「与 types.ts 可 diff」待人工核对后勾 |
| **P1-1** 同 `model:"jev"` 双候选，停 Vercel 落 Laya | ✅ 代码级 / 交互 **P2-7** | `abe4c16`；单测 `failover_429_falls_to_second_candidate_with_upstream_calls`（`upstream_calls==2`、upstream_model 改写断言）；集成 `failover_first_fail_second_ok_counts_two_calls`（wiremock 429→次候选 200，双方各 1 发）。**停真 Vercel 的交互验证 → P2-7** |
| **P1-1′** `local/*` prefix 命中 | ✅ | `select_prefix_local_star_hits`、`exact_and_prefix_both_contribute_candidates`（jev-core）；smoke 步骤 3 当日 `data=[jev, laya-english, local/*]` |
| **P1-2/5** 冻结双 trait + 黄金测试 + capability 注册制 | ✅ | `d0dbb5e`；黄金 3/3 当日复跑（仓外怪异 DTO `{"verdict":"yes","odds":0.8}` 零改内核注册命中、响应为标准 JevResponse、capability 自报无内核 match） |
| **P1-3** wiremock 一 429 一 200 → 客户端 200 且 `upstream_calls==2` | ✅ | `951c1a2`；`wiremock_429_then_200_client_200_upstream_calls_2` + `retry_exhausted_then_failover_to_next_candidate`（rs/crates/jev-adapters/tests/retry_wiremock.rs） |
| **P1-5′** 多跳别名链可解析 / 拒环加载 | ✅ | `select_multi_hop_alias_chain`、`upstream_model_overlay_along_chain`；`check_acyclic_rejects_cycle*` + config `cyclic_routes_rejected_at_load` + admin `put_routes_cycle_is_400_with_cycle_message` + **smoke 步骤 7**（400 含 环/cycle 且仓库示例 toml SHA256 未变） |
| **A3** HTTP 形状（health JSON / models OpenAI 风 / CORS 白名单） | ✅ | `5a3146f`；`health_shape_snapshot`（键集冻结 2 键）+ `models_shape_snapshot`（键集冻结 3 键、无 `models` 回潮）+ 集成 `health_json_shape_over_http`、`models_filters_unroutable_entries`；smoke 步骤 2/3 当日 200 |
| **A7** Admin API + 密钥防偷 | ✅ | `254b4ff`；单测①`get_providers_never_leaks_plaintext_key`（全响应无明文/掩码/键集 6 无 `api_key`）、③环 400、④热替换、⑥probe 形状冻结四键、⑦源码扫描 Serialize 面禁裸 `api_key`；集成 `admin_providers_over_http_has_no_plaintext`；**smoke 步骤 6/7/8** 当日复核；redact 9 单测（`redact_known_key_in_error_body` 等） |
| **A6** 本地 Laya 不重试（`retryable_status:[0]`） | ✅ | `951c1a2` 行为测试 + daemon 单测 `non_retryable_error_stops_even_with_on_error_next`（400 透传不 failover）；Laya 等本地类 429 透传非 503 为 A 线验收口径 |
| **A8** oneshot 集成基座 8 测 | ✅ | `9d5e06c`；`integration_http.rs` 8/8 当日随 workspace 142 绿（404/422/400/503/failover/过滤/掩码/health） |
| **06 §〇 4 条「做到」标准** | 见 §三 | — |

---

## 二、08 §4 前端 B 验收逐条

> 全部标注「**代码级 ✅ / 交互实测 → P2-7**」：组件与 API 调用面已合入，浏览器行为待终局 CDP + 人工实测。

| 条目 | 代码级 | 交互实测 |
|---|---|---|
| 提供商卡片可启停 / 粘贴密钥（密文回显）/ Probe 变红变绿 | ✅ `ProviderCard` / `KeyForm`（仅密文占位、无 Show 明文）/ `ProbeButton`；`ui/src/api/admin.ts` 打 `GET/PUT /v1/admin/providers`、`POST …/probe`；H3 `b4c5553` adminMode 默认 live | **P2-7**（devtools 无明文断言、开关 PUT 即时生效、Probe 灯号） |
| 拖线 → `[[routes]]` → `POST` 路由正确 | ✅ `BipartiteCanvas` / `EdgeInspector`（四字段）/ `RouteTableForm`（键盘等价）；debounce `PUT /v1/admin/routes`（mock→live 随 H3） | **P2-7**（拖线落 toml、真 POST 命中、删线立即失效） |
| 删线立即失效 | ✅ 删除乐观更新 + 失败回滚（B3 实现）；服务端整表替换热更由 `put_routes_success_hot_replaces_runtime_router` 锁定 | **P2-7** |
| toml 外部修改 → 横幅 → 重载后图与列表一致 | ✅ `ConflictBanner` + `useConfigConflict`（现役 GET 深比较，见 §七 备案） | **P2-7** |
| `/frontend-design` 交付物归档 `docs/design/` | ✅ 模拟稿 `8b5c5b1` → `design/01`；B6 视觉对齐 `18d9530` | 真调用产物（若后续再跑）→ P2-7 后补 |
| 类型单一来源（ts-rs 替换手写） | ✅ `5f8a74c`；`ui/src/api.ts` 全部 re-export `./generated/*`，`normalizeModels` 兼容桥已随 H1 删除 | — |
| Playground 分型与计量（B4） | ✅ `3c21c3c`：Noul 无 confidence 行、双键来源标注、null→`—` | **P2-7**（Run Jev 真实响应渲染） |

---

## 三、PROGRESS v0.1.0 四条「做到」标准复验

> 端口口径已按封存后变更：**一律 11435**（PROGRESS 正文 8765 为历史值）。

| # | 标准 | 本次复验 | 依赖 |
|---|---|---|---|
| 1 | `POST {model:"laya-english"}` → 真实路由到 Laya | **SKIP（live）**：本机 `127.0.0.1:18765` 当日不可达（smoke 步骤 9/10：probe `ok:false,status=0` → 步骤 10 SKIP）。**代码级 ✅**：路由/适配单测 + smoke 步骤 3 确认 `laya-english` 可路由（`upstream=laya`） | 需启动 Laya（P2-7 环境） |
| 2 | `POST {model:"typesafe-ai/jev"}` → 真实路由到 Vercel | **SKIP（live）**：`AI_GATEWAY_API_KEY` 未设置 → vercel 上游不注册（`/v1/models` 按设计过滤）。**代码级 ✅**：Vercel adapter/翻译单测全绿；smoke 步骤 9 显示 Vercel 端点**网络可达**（probe `ok:true,status=405`）——仅缺 key | 需提供 Vercel key（P2-7 环境） |
| 3 | 调用方发 `type: noul` → 网关自动翻译 boolean（Vercel 方言） | **SKIP（live，同上缺 key）**；**代码级 ✅**：`noul_to_boolean_wire`、`vercel_boolean_true/false_to_noul`、`boolean_input_answers_normalize_to_noul`（jev-adapters） | P2-7 |
| 4 | 只切 `model` 字段即可换上游 | **SKIP（live，组合依赖 1+2）**；**代码级 ✅**：smoke 步骤 3 当日证明按 model 出候选（`jev` 双候选 DAG、`local/*` prefix、过滤后每项 `upstream` 非空）+ registry failover 测试族 | P2-7 |

v0.1.0 时这四条曾端到端真值通过（`docs/PROGRESS.md` §一，8765 时代）；当前代码路径已重写（workspace/DAG/admin），**live 真值需在 P2-7 带 key + Laya 起服后复测**，故标 SKIP 而非沿用旧证据。

---

## 四、design/01 §10 DoD 状态 + 待 P2-7 的 8 项 CDP 清单

### §10 逐项

| DoD 项 | 状态 |
|---|---|
| 设计简报 / 原则 / tokens / IA / 三页规格 / 状态文案 / a11y / 实现约束成文 | ✅（契约复选框已勾，`8b5c5b1`） |
| `/frontend-design` 真调用产物与本稿 diff | ⏳ 未真调用；模拟稿在档（冲突以契约为准条款已成文） |
| Providers：卡片 + 开关 + 密文密钥 + Probe | 代码级 ✅ → **P2-7** |
| Routing：二部图拖线 ↔ toml 双向；键盘表单备选 | 代码级 ✅ → **P2-7** |
| Playground：Noul 无 confidence；估算标注；`—` 表 null | 代码级 ✅ → **P2-7** |
| 冲突横幅重载/覆盖 | 代码级 ✅ → **P2-7** |
| 无明文 key 进 DOM/console（契约 04） | API 层 ✅（当日 smoke 步骤 6 + 单测①⑦）→ DOM/console 断言 **P2-7** |

### 待 P2-7（B6 报告的 8 项 CDP 待验清单）

> B6 报告未落盘为独立文档（树内仅 `design/01` 与 commit `18d9530`）；下列 8 项按 **design/01 §10 未勾项 + 08 §4 B 验收 + 计划 P2-7 字面** 复原，作为 CDP 执行清单照录：

1. 启动三服务（daemon 11435 + `npm run dev` 5173 + Laya 18765）→ 浏览器打开 5173：**CDP `Runtime.exceptionThrown == 0`**，React root 渲染正常。
2. **三页可切**：Providers / Routing / Playground hash 路由往返，daemon 灯、版本显示正常。
3. **Providers 卡片启停**：开关 → `PUT /v1/admin/providers` 即时生效、失败回滚 toast。
4. **密钥密文**：粘贴测试 key（假值 `sk-test…`）→ 仅 `api_key_masked` 回显；**devtools DOM/console/network 全程无明文**。
5. **Probe 交互**：按钮 loading → 变绿（Vercel 405 连通）/ 变红（Laya 不可达文案），结果写卡片 footer。
6. **拖线 ↔ toml ↔ POST**：拖一条 `jev↔laya` 线 → debounce `PUT /v1/admin/routes` → toml 出现 `[[routes]]` → `POST /v1/systemone {model:"jev"}` 命中；**删线立即失效**；键盘 `RouteTableForm` 等价路径可用；400 环时边标红。
7. **冲突横幅**：外部手改 toml → 横幅「配置已在外部修改」→ 重载（默认）后图与列表一致 / 覆盖写回。
8. **端到端行为断言**：停 Vercel（或撤 key）→ `POST {model:"jev"}` failover 落 Laya 且 `upstream_calls≥2`；capability 422 断言；Run Jev 真实推理渲染（Noul 无 confidence、null→`—`）→ **交用户人工实测**。

---

## 五、contracts 01–06 §7/§8 状态表（**未改契约文件**）

| 契约 | 复选框项 | 状态 | 说明 |
|---|---|---|---|
| **01 §8** | ① 0.69 夹具 | ✅ | `noul_fixture_boolean_probability_069` + `vercel_probability_069_full_path` + example 当日 0.69 输出（前三项已验真） |
| | ② 0.96 `noul` 夹具 | ✅ | `noul_fixture_noul_096` |
| | ③ choice 三字段必填 / score f64 | ✅ | `choice_answer_three_required_fields`、`score_answer_score_is_f64` |
| | ④ 缺 criteria → 反序列化错误 | ✅ | `missing_criteria_rejected_with_upstream_message` + smoke 步骤 4（400） |
| | ⑤ ts-rs/Schema 与 jev-life 可 diff | ⏳ | 生成物已在（`7735cda`/`5f8a74c`）+ CI diff 门禁在（`656b4d4`）；**逐字 diff 勾选留所有者** |
| **02 §6** | ① 冻结 API 文档化 ② 分层铁律成文 | ✅ | 定稿已勾，维持 |
| | ③ crates 落地 + 黄金测试 | ⏳（实现✅） | `10c2fa9` workspace + 当日黄金 **3/3**；复选框未勾 → 待所有者 |
| **03 §7** | ① 成文 | ✅ | 定稿已勾 |
| | ② 双候选停 vercel 落 laya | ⏳ | 代码级 ✅（failover 测试族，§一）；真停 Vercel → **P2-7** |
| | ③ `local/*` prefix | ⏳（实现✅） | `select_prefix_local_star_hits` + smoke 步骤 3 |
| | ④ 多跳别名链 | ⏳（实现✅） | `select_multi_hop_alias_chain` |
| | ⑤ 配置加载拒环 | ⏳（实现✅） | `cyclic_routes_rejected_at_load` + admin 环 400 + smoke 步骤 7 |
| | ⑥ UI 拖线 ↔ toml 双向 | ⏳ | 代码级 ✅ → **P2-7** |
| **04 §7** | ① 成文 | ✅ | 定稿已勾 |
| | ② GET 无明文/无读回单测 | ⏳（实现✅） | 单测① + 集成 HTTP 级 + smoke 步骤 6（当日复跑） |
| | ③ redact 单测 | ⏳（实现✅） | `jev-core::redact` 9 测（含 `redact_known_key_in_error_body`） |
| | ④ 0600 检查告警 | ⏳（实现✅） | `check_config_perms_warn` 启动接线（main.rs）+ `check_config_perms_warns_on_wrong_mode` / Windows 降级 warning 单测 |
| **05 §6** | ① 端点与形状成文 | ✅ | 定稿已勾 |
| | ② OpenAPI/JSON Schema 进 `docs/contracts/schemas/` | ❌ **未做** | 目录不存在 → **遗留**（§七） |
| | ③ UI 生成类型替换手写 | ⏳（实现✅） | `5f8a74c`；api.ts 全 re-export、兼容桥已删 |
| | ④ 错误体 redact 单测 | ⏳（实现✅） | redact 测试族 + `error_response` 全路径过 redact（lib.rs） |
| | ⑤ admin 无明文泄露单测 | ⏳（实现✅） | 单测①⑦ + `admin_providers_over_http_has_no_plaintext` + smoke 6 |
| **06 §7** | ① IA/三页/DAG/密文/横幅成文 | ✅ | 定稿已勾 |
| | ② `/frontend-design` 产物合入 | ⏳ | 模拟稿已在 `design/01`（`8b5c5b1`）；**真调用产物**未跑 |
| | ③ 拖线 ↔ toml 双向**实测** | ⏳ | 代码级 ✅ → **P2-7** |
| | ④ 密钥密文**实测**（devtools 无明文） | ⏳ | API 层 ✅（当日实跑）→ DOM/console → **P2-7** |

---

## 六、测试与质量门禁总表

| 门禁 | 口径/命令 | 当日结果 | 备注 |
|---|---|---|---|
| workspace 单测+集成 | `cargo test --manifest-path rs/Cargo.toml --workspace` | **142/142** ✅ | 四 crate：protocol 23 + core 51 + adapters 21(+wiremock) + daemon 37+8 等 |
| feature `ts-rs` | `cargo test … --workspace --features ts-rs` | **165/165** ✅ | 含 ts-rs 导出测试；生成物 diff **空** |
| 黄金测试 | `powershell -File rs/scripts/golden-test.ps1` | **3/3** ✅ | 仓外 `my_lab_adapter`，一键重建可复制 |
| smoke（PS） | `powershell -File scripts/smoke.ps1` | **PASS 9/9, SKIP 1** ✅ | 11 步；SKIP=Laya 真实推理（环境） |
| smoke（bash） | `bash scripts/smoke.sh` | **PASS 9/9, SKIP 1** ✅ | 与 ps1 同断言集；本机 Git Bash 实跑 |
| example | `cargo run … -p jev-protocol --example basic_use` | ✅ exit 0 | `noul_probability = 0.69` 断言通过 |
| UI lint | `npm run lint --prefix ui` | ✅ | tsc --noEmit 0 错 |
| UI build | `npm run build --prefix ui` | ✅ | vite 55 modules → dist |
| CI | `.github/workflows/ci.yml`（`656b4d4`） | ✅ 配置在 | push/PR → main：workspace 测试 + ts-rs **生成物 diff 门禁** + ui lint/build；badge 已进根 README |

---

## 七、已知偏差与遗留（备案清单）

| # | 项 | 性质 | 处置 |
|---|---|---|---|
| 1 | **Admin CORS 沿用 5173 白名单** vs 契约 05 §5「Admin 默认不跨源；仅同主机 UI」 | 偏差（A7 已备案于 `build_app` 注释；不擅自收紧） | **待用户裁决** |
| 2 | `family=4` IPv4-only 开关未做 | 07 P2 可选项（Cloudflare IPv6 历史问题） | 非阻塞，可延后 |
| 3 | sticky HTTP 入口未接：`RouteCtx.sticky_key` 恒 `None`（`plain_ctx()`） | 功能半接（单测已锁语义：`sticky_session_hoists_remembered_candidate`、`replace_edges_clears_sticky_memory`） | 需 sticky 时再开 header/入口 |
| 4 | 冲突横幅现役 = **GET 深比较**（mtime/hash 方案待 admin API 扩展） | 降级实现（B2 已注明一期口径） | admin 扩展 mtime 后升级 |
| 5 | toml 写回**不保留注释**（toml crate 往返限制） | 平台限制（providers.example.toml 注释头会随 PUT 丢失——smoke 用临时副本保护仓库文件） | 已在配置注释与 README 注明 |
| 6 | **B6 的 8 项 CDP 待验**（§四清单） | 未实测 | **P2-7**（本报告移交） |
| 7 | Laya（18765）/ Vercel（key）可用性依赖本机环境 | live 验收 4 条当日 **SKIP** | **P2-7** 起服 + 配 key 后复测 |
| 8 | **OpenAPI/JSON Schema 未提交** `docs/contracts/schemas/`（05 §6 第②项 ❌） | 遗留 | 非 v0.5.0 阻塞项，建议 tag 后立独立任务 |
| 9 | 契约 §7/§8 多项「实现✅、复选框未勾」 | 流程项 | **勾选权留用户/契约所有者**（本报告只列状态表，未改 `docs/contracts/**`） |

---

## 八、结论：是否具备打 tag `v0.5.0-stable` 条件

**是——代码级与测试级 DoD 已全部达成；打 tag 前仅剩两项计划内任务：**

1. **P2-7 终局实测**（用户裁决的验收形态）：起 daemon 11435 + 配 Vercel key + 起 Laya 18765 + `npm run dev` 5173 → 浏览器 + CDP 跑 §四 8 项清单（0 报错、三页、Run Jev、拖线 PUT、停 Vercel failover、422、密文 devtools）→ **交用户人工实测**；通过后 PROGRESS 四条 live 真值复测转 ✅。
2. **P2-8 打 tag 并 push**（归主会话）。

**不阻塞 tag 但需在 tag 说明或后续任务中备案**：§七 #1 admin CORS 裁决、#8 OpenAPI/JSON Schema、#9 契约复选框勾选（建议用户对照 §五状态表逐项勾）。

**一句话**：五道当日门禁（142 / 165+diff 空 / 黄金 3/3 / UI lint+build / smoke 9/9×双实现）全绿，A 验收与 B 代码级全数有据；**v0.5.0-stable 差的只是浏览器里的最后一步（P2-7）与 tag 本身（P2-8）。**

— Phase2-Agent · 2026-09-23

---

## 九、附：P2-7 CDP 自测结果（2026-09-23 · 主会话执行）

**PASS 12/12**（驱动：`E:\tmp\jev-cdp-e2e.py`，Edge headless + CDP :9223 + websockets 原生实现，零 playwright 依赖；证据归档 [`design/screenshots-v0.5.0/`](./design/screenshots-v0.5.0/)，含 `cdp-e2e-report.json`）：

| # | 项 | 结果 |
|---|---|---|
| ① | 三服务就绪 + 页面载入 + daemon 灯 | ✅（`01-*`） |
| ② | 三页可切 | ✅ providers / routing / playground（`02-*`） |
| ③ | 卡片启停 UI↔PUT↔GET 往返 + **整表 PUT 后密钥保留** | ✅ aria `true→false→true`，`api_key_set=True` |
| ④ | 密钥零明文（API 响应 + DOM，fake key `sk-…cafe`） | ✅ 三查全 false |
| ⑤ | Probe 双卡实变（dead vercel 红态 / live laya 绿态） | ✅（`04-*`） |
| ⑥ | 拖线 → debounce PUT → **配置文件真值** → 删线回真值 | ✅ 5/5 子断言（`05/06-*`） |
| ⑦ | 外部改 toml → 冲突横幅 → 重载生效 | ✅（`07-*`） |
| ⑧a | Run Jev：默认 choice 示例 + **注入 noul 题 UI 真跑** | ✅ `noul=0.0001`（校准合理）（`08-*`） |
| ⑧b | `jev` 双候选 failover | ✅ **`upstream_calls=4`**（dead vercel ×3 A6 退避 → laya ×1，与 A6 模型精确一致）（`09-*`） |
| ⑧c | 负例 404 / 400 | ✅（**422 线上不可复现**：vercel+laya 能力集无缺口，由集成测试 `capability_mismatch_is_422_without_real_send` 护栏覆盖） |
| ⑨ | 全程 `Runtime.exceptionThrown=0` 且 `console.error=0` | ✅ |

**环境注记**：Laya 以仓外完整快照 `E:/hf-cache/hub/models--convaiinnovations--laya/snapshots/1c5edc17…` 本地直载（HF 上游 revision 已漂移，在线补齐非必需）；demo 配置为**仓外临时副本**（vercel 指向 `127.0.0.1:59999` 死网关 + fake key，制造 failover / 红灯 / 掩码演示面），仓库 `providers.example.toml` 全程零污染；**真实 Vercel key 未配**（环境无 `AI_GATEWAY_API_KEY`）→ PROGRESS 四条 live 中「Vercel 直连」与「noul→boolean 实网翻译」仍待配 key 复测。

**状态**：CDP 自测 ✅ → 移交用户人工验收（`/show` 打开 `http://127.0.0.1:5173`）→ 通过后按 P2-8 打 tag。

**验收反馈迭代复跑（2026-09-23）**：用户 4 条 Playground 反馈落地后（`6247e1c` 样例/模型解耦 + 表单编辑器、`f457c82` Form/JSON 真双视图、`03f4b0c` 样例库扩至 14 题——成语/诗句填字、2022 新高考 I 卷 Q1/Q5 lab 同源真题、网页点击目标）→ 全量 `lint+build` 0 错 → **CDP E2E 复跑 PASS 12/12**（截图已刷新覆盖归档；`E:\tmp\jev-cdp-e2e.py` 已适配注入前自动切 JSON tab）。
