# Jev-Switch 功能特性契约（正文）

> **作者**：Mimo-V2.6-Pro  
> **日期**：2026-09-23  
> **上游**：`../07-REVIEW-v0.1.0-mvp.md`、`../08-CONTRACT-功能特性契约与施工计划.md`  
> **状态**：**Phase 0 完成** —— 六份契约正文齐备，可并行进入后端 A / 前端 B。  
> **效力**：与 `07-`/`08-` 冲突时，**本目录正文更具体者优先**；语义争议回看 `08-` 对齐定稿。

## 阅读顺序

| # | 文件 | 管什么 | 主要读者 |
|---|---|---|---|
| 1 | [`01-协议契约.md`](./01-协议契约.md) | Jev 内核类型形状、字段不变量、与 jev-life 对齐 | 后端 A、任何依赖方 |
| 2 | [`02-扩展点契约.md`](./02-扩展点契约.md) | serde 同构：冻结 trait、黄金测试、红线 | 后端 A、第三方 adapter 作者 |
| 3 | [`03-路由契约.md`](./03-路由契约.md) | **模型路由 DAG**、`[[routes]]`、选路语义 | 后端 A、前端 Routing 页 |
| 4 | [`04-密钥与防偷.md`](./04-密钥与防偷.md) | 明文 toml 默认档 + 防偷红线 + 借鉴清单 | 后端 A、前端 Providers 页 |
| 5 | [`05-HTTP契约.md`](./05-HTTP契约.md) | `/v1/*` 请求响应形状、错误体、CORS | 前后端联调 |
| 6 | [`06-前端交互契约.md`](./06-前端交互契约.md) | CC Switch 范式、三页 IA、DAG/二部图编辑器 | 前端 B（`/frontend-design`） |

## 对齐定稿速查（不可再开）

> 本表为 `../08-CONTRACT-功能特性契约与施工计划.md` §0 的**镜像速查**；内容不一致时以 08 原表为准，修订须两边同步。

| Q | 决定 |
|---|---|
| Q1 | 编译期 trait 同构（A）；内核与传参零改 |
| Q2 | 交互+视觉先靠 CC Switch（A+B）；测试稳定后再 Tauri（C） |
| Q3 | 契约 → 并行 AB → 双剑合璧 |
| Q4 | 密钥明文 toml + `.gitignore`；**UI 必须密文** |
| Q5 | **以文件为准**；外部修改 → UI 重载/覆盖横幅 |
| Q6 | 防偷强化：上游 key 不出 daemon；无读回明文 API；日志 redact |

## 施工门禁

1. **禁止**在六份契约未读前改 `rs/` 或 `ui/`
2. **禁止**加第 5 个上游，直到 `02-` 黄金测试通过
3. **禁止** `git push` 未授权；密钥真实值不进上下文/日志/UI 明文
4. 只写 `jev-switch/`；不碰 `~/Rime/`、`jev-life/`、`jev-rime/`

— Mimo-V2.6-Pro
